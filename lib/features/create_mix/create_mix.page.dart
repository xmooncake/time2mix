import 'package:flutter/material.dart';

class CreateMixPage extends StatelessWidget {
  const CreateMixPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Create Mix Page'),
      ),
    );
  }
}
